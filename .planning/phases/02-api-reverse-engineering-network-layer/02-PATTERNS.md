# Phase 2: API Reverse-Engineering & Network Layer — Pattern Map

**Mapped:** 2026-04-28
**Files analyzed:** ~46 new/modified files across 3 modules + tooling + CI
**Analogs found:** 14 strong analogs covering 100% of file roles (every file maps to either a Phase 1 internal analog OR a verbatim RESEARCH.md code example)

## File Classification

### Module 1: `:core:database` (NEW Gradle module)

| New File | Role | Data Flow | Closest Analog | Match Quality |
|---|---|---|---|---|
| `core/database/build.gradle.kts` | config (Gradle) | build-graph | `core/platform/build.gradle.kts` | exact role; needs Room/KSP additions |
| `core/database/src/commonMain/kotlin/.../core/database/JournalDatabase.kt` | model (Room `@Database`) | persistence | RESEARCH.md Code Example #1 + #3 (no Phase 1 analog — first DB) | new pattern; RESEARCH-driven |
| `core/database/src/commonMain/kotlin/.../core/database/JournalDatabaseConstructor.kt` | model (Room `expect object`) | persistence | `core/platform/.../UrlOpener.kt` (expect declaration shape) | role-match; spec from Room KMP guide |
| `core/database/src/commonMain/kotlin/.../core/database/entity/CookieEntity.kt` | model (`@Entity`) | persistence | RESEARCH.md Pattern 2 + Code Example excerpt (no Phase 1 analog) | new |
| `core/database/src/commonMain/kotlin/.../core/database/dao/CookieDao.kt` | dao (Room `@Dao`) | CRUD | RESEARCH.md Pattern 2 (verbatim) | new |
| `core/database/src/commonMain/kotlin/.../core/database/DatabaseFactory.kt` | platform-abstraction (`expect object`) | factory | `core/platform/.../UrlOpener.kt` | exact pattern (D-31 expect/actual) |
| `core/database/src/androidMain/kotlin/.../core/database/DatabaseFactory.android.kt` | platform-impl (`actual`) | factory | `core/platform/.../UrlOpener.android.kt` | exact pattern (Context-bridging shape) |
| `core/database/src/iosMain/kotlin/.../core/database/DatabaseFactory.ios.kt` | platform-impl (`actual`) | factory | `core/platform/.../UrlOpener.ios.kt` + RESEARCH.md Code Example #3 | exact pattern (Foundation imports) |
| `core/database/src/androidMain/AndroidManifest.xml` | config (manifest fragment) | manifest-merge | `composeApp/src/androidMain/AndroidManifest.xml` (allowBackup target) + RESEARCH.md Code Example #2 | exact pattern; D-21 |
| `core/database/src/commonTest/kotlin/.../JournalDatabaseTest.kt` | test (commonTest) | test-only | RESEARCH.md "Wave 0 Gaps" list (no Phase 1 analog) | new |
| `core/database/src/androidUnitTest/kotlin/.../DatabaseFactoryAndroidTest.kt` | test (Robolectric) | test-only | `composeApp/src/androidUnitTest/.../AppTestAndroid.kt` | role-match (Robolectric pattern) |
| `core/database/src/iosTest/kotlin/.../DatabaseFactoryIosTest.kt` | test (iosTest) | test-only | `composeApp/src/iosTest/.../AppTest.kt` | role-match (iosTest runner pattern) |
| `core/database/schemas/.../1.json` | data (Room schema export) | build-artifact | n/a (auto-generated by `room { schemaDirectory(...) }`) | n/a |

### Module 2: `:core:network` (FILL Phase 1 skeleton)

| New / Modified File | Role | Data Flow | Closest Analog | Match Quality |
|---|---|---|---|---|
| `core/network/build.gradle.kts` (MODIFY) | config | build-graph | `core/platform/build.gradle.kts` | exact role; add ktor + project deps |
| `core/network/src/commonMain/kotlin/.../core/network/HttpClientFactory.kt` | factory (per-account cache) | factory + lifecycle | RESEARCH.md Pattern 1 (verbatim) | new |
| `core/network/src/commonMain/kotlin/.../core/network/cookies/RoomCookiesStorage.kt` | adapter (Ktor `CookiesStorage`) | request-response (cookie I/O) | RESEARCH.md Pattern 2 (verbatim) | new |
| `core/network/src/commonMain/kotlin/.../core/network/plugins/HttpRequestRedactor.kt` | utility (regex masker) | transform | RESEARCH.md Code Example #4 (verbatim) | new |
| `core/network/src/commonMain/kotlin/.../core/network/plugins/SanitizeHeader.kt` | utility (header redactor) | transform | RESEARCH.md Pattern 1 lines 533-538 (sanitizeHeader inline) | new |
| `core/network/src/commonMain/kotlin/.../core/network/plugins/AversAuthInterceptor.kt` | interceptor (HttpSend) | request-response | RESEARCH.md Code Example #5 (verbatim) | new |
| `core/network/src/commonMain/kotlin/.../core/network/credentials/CredentialProvider.kt` | interface (suspend get) | request-response | `core/platform/.../UrlOpener.kt` (interface/expect shape with KDoc preamble) | role-match; pure interface |
| `core/network/src/commonMain/kotlin/.../core/network/lifecycle/AccountDataPurger.kt` | interface + default impl (lifecycle) | event-driven (logout) | RESEARCH.md Pattern 1 (`HttpClientFactory.evict(accountId)`) + D-22 (no Phase 1 analog) | new |
| `core/network/src/commonMain/kotlin/.../core/network/logging/KermitKtorLogger.kt` | utility (Logger adapter) | event-driven | `core/platform/.../UrlOpener.android.kt` (Kermit usage with TAG) | role-match; tiny adapter |
| `core/network/src/commonTest/kotlin/.../HttpClientFactoryTest.kt` | test | test-only | RESEARCH.md Wave 0 Gaps + `composeApp/src/commonTest/.../AppTestHelpers.kt` | role-match (commonTest pattern) |
| `core/network/src/commonTest/kotlin/.../cookies/RoomCookiesStorageTest.kt` | test | test-only | same | role-match |
| `core/network/src/commonTest/kotlin/.../plugins/HttpRequestRedactorTest.kt` | test (parameterized) | test-only | same | role-match |
| `core/network/src/commonTest/kotlin/.../plugins/AversAuthInterceptorTest.kt` | test (stateful MockEngine) | test-only | RESEARCH.md "Wave 0 Gaps" + Pitfall #4 (skip-login-endpoint) | new |
| `core/network/src/commonTest/kotlin/.../lifecycle/AccountDataPurgerTest.kt` | test | test-only | same | new |

### Module 3: `:core:api-avers-v4` (NEW Gradle module)

| New File | Role | Data Flow | Closest Analog | Match Quality |
|---|---|---|---|---|
| `core/api-avers-v4/build.gradle.kts` | config | build-graph | `core/platform/build.gradle.kts` | exact role; needs serialization + ktor + project deps |
| `core/api-avers-v4/src/commonMain/kotlin/.../core/api/avers/v4/AversApi.kt` | service (interface + impl) | request-response | RESEARCH.md Pattern 3 (verbatim `fetchGrades` excerpt) | new |
| `core/api-avers-v4/src/commonMain/kotlin/.../core/api/avers/v4/AversAuthApi.kt` | service (login/logout) | request-response | RESEARCH.md Pattern 1 (loginCall lambda from Code Example #5) | new |
| `core/api-avers-v4/src/commonMain/kotlin/.../core/api/avers/v4/result/ApiResult.kt` | model (sealed class) | type-only | RESEARCH.md Code Example #6 (verbatim) | new |
| `core/api-avers-v4/src/commonMain/kotlin/.../core/api/avers/v4/result/AversApiError.kt` | model (sealed class) | type-only | RESEARCH.md Code Example #6 (verbatim D-11) | new |
| `core/api-avers-v4/src/commonMain/kotlin/.../core/api/avers/v4/dto/envelope/AversEnvelope.kt` | utility (`{success, data}` unwrap) | transform | RESEARCH.md Pattern 3 (envelope check excerpt) | new |
| `core/api-avers-v4/src/commonMain/kotlin/.../core/api/avers/v4/dto/Period.kt` | model (data class) | type-only | (kotlinx-datetime LocalDate-based) | new |
| `core/api-avers-v4/src/commonMain/kotlin/.../core/api/avers/v4/dto/{Grade,Lesson,Homework,Attendance,Message}Dto.kt` | model (`@Serializable`) | persistence (decode-only) | RESEARCH.md Pattern 3 + Pitfall #5 (datetime caveat) | new (shapes await HAR capture) |
| `core/api-avers-v4/src/commonMain/kotlin/.../core/api/avers/v4/antibot/AntiBotDetector.kt` | utility (HTML sniffer) | transform | RESEARCH.md Pattern 3 (`antiBotDetector.isCaptcha(raw)`) | new |
| `core/api-avers-v4/src/commonMain/kotlin/.../core/api/avers/v4/killswitch/KillSwitchClient.kt` | service (fail-open fetcher) | request-response | RESEARCH.md Code Example #7 (verbatim) | new |
| `core/api-avers-v4/src/commonMain/kotlin/.../core/api/avers/v4/killswitch/KillSwitchConfig.kt` | model (`@Serializable`) | type-only | RESEARCH.md Code Example #7 (verbatim) | new |
| `core/api-avers-v4/src/commonTest/kotlin/.../fixtures/HarReplayMockEngine.kt` | utility (test-only fixture loader) | test-only | RESEARCH.md Code Example #9 (verbatim) | new |
| `core/api-avers-v4/src/commonTest/kotlin/.../endpoint/{Login,Grades,Schedule,Homework,Attendance,Messages}EndpointTest.kt` | test (×6 endpoints × 2 accounts) | test-only | same | new |
| `core/api-avers-v4/src/commonTest/kotlin/.../result/AversApiErrorMappingTest.kt` | test | test-only | same | new |
| `core/api-avers-v4/src/commonTest/kotlin/.../killswitch/KillSwitchClientTest.kt` | test | test-only | same | new |
| `core/api-avers-v4/src/commonTest/kotlin/.../antibot/AntiBotDetectorTest.kt` | test | test-only | same | new |

### Top-Level Files (Modify / New)

| File | Role | Data Flow | Closest Analog | Match Quality |
|---|---|---|---|---|
| `gradle/libs.versions.toml` (MODIFY) | config (version catalog) | build-graph | existing `gradle/libs.versions.toml` | self-analog (extend in place) |
| `settings.gradle.kts` (MODIFY) | config | build-graph | existing `settings.gradle.kts` | self-analog (add 2 includes) |
| `tools/sanitize-har.py` (NEW) | utility (Python script) | file-I/O / transform | RESEARCH.md Code Example #8 (verbatim) | new |
| `tools/sanitize-rules.yaml` (NEW) | config (PII rules) | data | RESEARCH.md Code Example #8 ref | new |
| `tools/README.md` (NEW) | docs | doc-only | n/a | new |
| `fixtures/raw/.gitkeep` (NEW) | placeholder | fs | n/a | new |
| `fixtures/sanitized/account-{A,B}/{login,grades,schedule,homework,attendance,messages}.har` (12 NEW) | data (test fixtures) | data | n/a — D-04 storage layout | new |
| `fixtures/README.md` (NEW) | docs | doc-only | n/a | new |
| `docs/aversApiV4_23813.md` (NEW) | docs (human-readable contract) | doc-only | n/a (contract emerges from HAR) | new |
| `docs/api-config.json` (NEW) | data (kill-switch config) | static-asset | `docs/privacy/index.html` (lives next to it; pages.yml deploys it) | role-match (GH Pages asset) |
| `tests/sanitize-har-canary.sh` (NEW) | test (CI lint) | shell | n/a (Phase 1 has no shell canary) | new |
| `tests/log-redactor-canary.sh` (NEW) | test (CI lint) | shell | n/a | new |
| `.github/workflows/ci.yml` (MODIFY) | config (CI) | build-graph | existing `.github/workflows/ci.yml` | self-analog (add steps) |
| `composeApp/PrivacyInfo.xcprivacy` (POSSIBLY MODIFY) | config (Apple privacy manifest) | static-asset | existing `composeApp/PrivacyInfo.xcprivacy` | self-analog (add reason codes if Room/Ktor flag any) |
| `composeApp/src/androidMain/AndroidManifest.xml` (POSSIBLY MODIFY) | config (manifest) | manifest-merge | existing manifest | self-analog (add `allowBackup="false"` per D-21 — but RESEARCH.md Example #2 places it in `:core:database/src/androidMain/AndroidManifest.xml` instead, leaving composeApp untouched) |
| `.planning/ROADMAP.md` (MODIFY) | docs | doc-only | n/a | self-analog (mitmproxy → Chrome DevTools edit per D-02) |
| `.gitignore` (MODIFY) | config | fs | existing `.gitignore` | self-analog (add `fixtures/raw/`, `.env.local`) |

---

## Pattern Assignments

### `core/database/build.gradle.kts` (config, build-graph)

**Analog:** `core/platform/build.gradle.kts` (Phase 1 baseline KMP-only module — same role, no compose deps)

**Plugins block pattern** (`core/platform/build.gradle.kts:1-4`):
```kotlin
plugins {
    id("lintech-kmp")
    id("lintech-test")
}
```
Phase 2 extension per RESEARCH.md Code Example #1 (lines 807-812): add `alias(libs.plugins.ksp)` + `alias(libs.plugins.androidxRoom)`. NO `id("lintech-compose")` — non-UI module (CLAUDE.md §Module structure BLOCKER 1 fix iter 1; RESEARCH.md Pitfall #8).

**KMP targets + iOS framework pattern** (`core/platform/build.gradle.kts:6-14`):
```kotlin
kotlin {
    androidTarget()
    listOf(iosX64(), iosArm64(), iosSimulatorArm64()).forEach { iosTarget ->
        iosTarget.binaries.framework {
            baseName = "platform"
            isStatic = true
        }
    }
    // ...
}
```
Phase 2: change `baseName = "database"` (analog to `:core:network` build.gradle.kts:11 which already uses `baseName = "network"`).

**commonMain dependencies pattern** (`core/platform/build.gradle.kts:15-27`):
```kotlin
sourceSets {
    commonMain.dependencies {
        implementation(libs.kermit)
    }
    androidMain.dependencies {
        implementation(libs.androidx.core.ktx)
    }
}
```
Phase 2 per RESEARCH.md Code Example #1 (lines 823-835): inside `kotlin { sourceSets { ... } }` add `commonMain.dependencies { implementation(libs.androidx.room.runtime); implementation(libs.androidx.sqlite.bundled); implementation(libs.kotlinx.coroutines.core); implementation(libs.kotlinx.datetime); implementation(project(":core:platform")) }`.

**`android { }` block pattern** (`core/platform/build.gradle.kts:30-34`):
```kotlin
android {
    namespace = "io.github.chudoxl.linteh.journal.core.platform"  // D-08, D-09
    compileSdk = 35
    defaultConfig { minSdk = 26 }
}
```
Phase 2: change namespace to `"io.github.chudoxl.linteh.journal.core.database"`.

**Phase-2-specific additions** (RESEARCH.md Code Example #1 lines 837-847; NO Phase 1 analog):
```kotlin
room {
    schemaDirectory("$projectDir/schemas")
}

// Per-target KSP — required for Room KMP (RESEARCH.md Pitfall #3)
dependencies {
    add("kspAndroid", libs.androidx.room.compiler)
    add("kspIosSimulatorArm64", libs.androidx.room.compiler)
    add("kspIosX64", libs.androidx.room.compiler)
    add("kspIosArm64", libs.androidx.room.compiler)
}
```

---

### `core/database/src/commonMain/kotlin/.../core/database/DatabaseFactory.kt` (platform-abstraction, factory)

**Analog:** `core/platform/src/commonMain/kotlin/io/github/chudoxl/linteh/journal/core/platform/UrlOpener.kt` (Phase 1 D-31 expect/actual template)

**Imports + KDoc + expect-declaration pattern** (`UrlOpener.kt:1-16`):
```kotlin
package io.github.chudoxl.linteh.journal.core.platform

/**
 * Открывает URL в системном браузере.
 *
 * Phase 1: используется кнопкой «Открыть» на Hello LinTech screen для Privacy Policy URL.
 * Phase 4+: реиспользуется для всех внешних ссылок (поддержка, AVERS-сайт, ссылки в сообщениях
 * учителей и т. д.).
 *
 * Security note (Phase 4 TODO): на момент Phase 1 единственный caller — Hello LinTech composable
 * с compile-time const URL из strings.xml. Нет user-input → нет URL-injection vector.
 * ...
 */
expect fun openUrl(url: String)
```

**Phase 2 adaptation** (per D-19 + RESEARCH.md Code Example #3 — note: Room KMP uses `expect object` rather than `expect fun`):
- Package: `io.github.chudoxl.linteh.journal.core.database`
- Shape: `expect object DatabaseFactory { fun create(accountId: String): JournalDatabase }`
- KDoc must reference: D-16 (`journal_${accountId}.db` filename), D-19 (Phase 2 `accountId="default"`), D-20 (iOS file protection), D-22 (Phase 3 transition note).

**Companion expect (Room KMP standard — RESEARCH.md anti-pattern callout):**
```kotlin
// expect class JournalDatabase is wrong — fights Koin/DI
// Correct: plain class JournalDatabase + expect object JournalDatabaseConstructor
expect object JournalDatabaseConstructor : RoomDatabaseConstructor<JournalDatabase>
```

---

### `core/database/src/androidMain/kotlin/.../core/database/DatabaseFactory.android.kt` (platform-impl, factory)

**Analog:** `core/platform/src/androidMain/kotlin/io/github/chudoxl/linteh/journal/core/platform/UrlOpener.android.kt`

**Context-bridging pattern** (`UrlOpener.android.kt:28-39`) — reuse the existing `applicationContextHolder` + `initApplicationContext(...)` mechanism rather than re-introducing top-level mutable globals:
```kotlin
private var applicationContextHolder: Context? = null

fun initApplicationContext(context: Context) {
    check(applicationContextHolder == null) {
        "androidApplicationContext already initialized"
    }
    applicationContextHolder = context.applicationContext
}

private val androidApplicationContext: Context
    get() = applicationContextHolder
        ?: error("Call initApplicationContext() in Application.onCreate() first")
```

**Phase 2 strategy:** Since `:core:database` depends on `:core:platform` (RESEARCH.md "Module dependency graph" line 469-477), the Android `DatabaseFactory.android.kt` should import the existing `androidApplicationContext` from `:core:platform` rather than redeclaring the holder. **Recommended planner action:** expose a `fun getApplicationContext(): Context` accessor in `:core:platform/androidMain` (small refactor) and have `DatabaseFactory.android.kt` call it. This avoids duplicating the BL-02 pattern.

**actual + Logger pattern** (`UrlOpener.android.kt:51-67`):
```kotlin
private const val TAG = "UrlOpener"

actual fun openUrl(url: String) {
    // ...
    runCatching { androidApplicationContext.startActivity(intent) }
        .onFailure { Logger.w(TAG, it) { "startActivity failed for: $url" } }
}
```

**Phase 2 adaptation** (combine with `Room.databaseBuilder<JournalDatabase>(context, dbFile.absolutePath)` per Room KMP guide cited in RESEARCH.md Standard Stack):
```kotlin
actual object DatabaseFactory {
    actual fun create(accountId: String): JournalDatabase {
        val context = getApplicationContext()  // from :core:platform
        val dbFile = context.getDatabasePath("journal_${accountId}.db")
        return Room.databaseBuilder<JournalDatabase>(
            context = context,
            name = dbFile.absolutePath,
        )
            .setDriver(BundledSQLiteDriver())
            .setQueryCoroutineContext(Dispatchers.IO)
            .build()
    }
}
```

---

### `core/database/src/iosMain/kotlin/.../core/database/DatabaseFactory.ios.kt` (platform-impl, factory)

**Analog:** `core/platform/src/iosMain/kotlin/io/github/chudoxl/linteh/journal/core/platform/UrlOpener.ios.kt` (Foundation imports + Kermit logging + actual shape)

**Imports + actual + defensive guard pattern** (`UrlOpener.ios.kt:1-26`):
```kotlin
package io.github.chudoxl.linteh.journal.core.platform

import co.touchlab.kermit.Logger
import platform.Foundation.NSURL
import platform.UIKit.UIApplication

/**
 * **WR-08 mitigation:** silent-failure ⇒ logged-warning. Прежде iOS-actual возвращал
 * `?: return` без диагностики...
 */
private const val TAG = "UrlOpener"

actual fun openUrl(url: String) {
    val nsUrl = NSURL.URLWithString(url) ?: run {
        Logger.w(TAG) { "Invalid URL, skipping openUrl: $url" }
        return
    }
    UIApplication.sharedApplication.openURL(...)
}
```

**Phase 2 adaptation** — RESEARCH.md Code Example #3 (lines 873-915) verbatim:
```kotlin
package io.github.chudoxl.linteh.journal.core.database

import androidx.room.Room
import androidx.room.RoomDatabase
import androidx.sqlite.driver.bundled.BundledSQLiteDriver
import kotlinx.cinterop.ExperimentalForeignApi
import kotlinx.coroutines.Dispatchers
import platform.Foundation.NSDocumentDirectory
import platform.Foundation.NSFileManager
import platform.Foundation.NSFileProtectionComplete
import platform.Foundation.NSFileProtectionKey
import platform.Foundation.NSURL
import platform.Foundation.NSUserDomainMask

@OptIn(ExperimentalForeignApi::class)
actual object DatabaseFactory {
    actual fun create(accountId: String): JournalDatabase {
        val docsDir = NSFileManager.defaultManager.URLForDirectory(
            directory = NSDocumentDirectory,
            inDomain = NSUserDomainMask,
            appropriateForURL = null,
            create = true,
            error = null,
        )!!

        // D-20: ensure parent directory has NSFileProtectionComplete attribute
        NSFileManager.defaultManager.setAttributes(
            mapOf(NSFileProtectionKey to NSFileProtectionComplete),
            ofItemAtPath = docsDir.path!!,
            error = null,
        )

        val dbPath = "${docsDir.path}/journal_${accountId}.db"

        return Room.databaseBuilder<JournalDatabase>(name = dbPath)
            .setDriver(BundledSQLiteDriver())
            .setQueryCoroutineContext(Dispatchers.IO)
            .build()
    }
}
```

Add a `private const val TAG = "DatabaseFactory"` and Kermit `Logger.i(TAG) { "Creating DB at $dbPath" }` to symmetrize with `UrlOpener.ios.kt:14` and aid Phase 4 reverse-engineering iOS-only debug.

---

### `core/database/src/androidMain/AndroidManifest.xml` (config, manifest-merge)

**Analog:** `composeApp/src/androidMain/AndroidManifest.xml` (Phase 1 reference — currently has no `android:allowBackup`)

**Existing manifest pattern** (`composeApp/src/androidMain/AndroidManifest.xml:1-19`):
```xml
<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <application
        android:name=".MainApplication"
        android:label="ЛИнТех Дневник"
        android:icon="@android:drawable/sym_def_app_icon"
        android:supportsRtl="true">
        <activity ...>
            ...
        </activity>
    </application>
</manifest>
```

**Phase 2 — manifest-fragment pattern from RESEARCH.md Code Example #2 (lines 858-867):**
```xml
<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
          xmlns:tools="http://schemas.android.com/tools">
    <application
        android:allowBackup="false"
        android:fullBackupContent="false"
        tools:replace="android:allowBackup" />
</manifest>
```

**Choice point for planner:** RESEARCH.md places this fragment in `:core:database/src/androidMain/AndroidManifest.xml`. Manifest merger will fold `allowBackup="false"` into the final app manifest. Alternative is to edit `composeApp/src/androidMain/AndroidManifest.xml` directly — simpler, but couples D-21 with the app module rather than the data module. **Recommended:** keep RESEARCH.md placement (closer to the data it protects).

---

### `core/network/build.gradle.kts` (config, build-graph) — MODIFY

**Analog (self):** existing `core/network/build.gradle.kts` (Phase 1 empty skeleton)

**Current state** (`core/network/build.gradle.kts:1-21`):
```kotlin
plugins {
    id("lintech-kmp")
    id("lintech-test")
}

kotlin {
    androidTarget()
    listOf(iosX64(), iosArm64(), iosSimulatorArm64()).forEach { iosTarget ->
        iosTarget.binaries.framework {
            baseName = "network"
            isStatic = true
        }
    }
}

android {
    namespace = "io.github.chudoxl.linteh.journal.core.network"
    compileSdk = 35
    defaultConfig { minSdk = 26 }
}
```

**Phase 2 extension** — extend `kotlin { sourceSets { ... } }` per RESEARCH.md Pattern 1 + Standard Stack Installation diff (lines 220-243):
```kotlin
sourceSets {
    commonMain.dependencies {
        implementation(libs.ktor.client.core)
        implementation(libs.ktor.client.content.negotiation)
        implementation(libs.ktor.serialization.json)
        implementation(libs.ktor.client.logging)
        implementation(libs.kotlinx.serialization.json)
        implementation(libs.kotlinx.coroutines.core)
        implementation(libs.kotlinx.datetime)
        implementation(libs.kermit)
        implementation(project(":core:platform"))
        implementation(project(":core:database"))
    }
    androidMain.dependencies {
        implementation(libs.ktor.client.okhttp)
    }
    iosMain.dependencies {
        implementation(libs.ktor.client.darwin)
    }
    commonTest.dependencies {
        implementation(libs.ktor.client.mock)
    }
}
```

---

### `core/network/src/commonMain/kotlin/.../core/network/HttpClientFactory.kt` (factory, lifecycle)

**Analog:** RESEARCH.md Pattern 1 (lines 481-590) — verbatim full pseudocode block. NO Phase 1 internal analog (first Ktor module).

**Direct copy targets** (lines 489-590):
- Class signature with constructor params (engine, cookiesStorageProvider, credentialProvider, redactor, baseUrl default, isDebug)
- `private val mutex = Mutex(); private val clients = mutableMapOf<String, HttpClient>()`
- `suspend fun forAccount(accountId): HttpClient = mutex.withLock { clients.getOrPut(accountId) { buildClient(accountId) } }`
- `suspend fun evict(accountId)` for `AccountDataPurger.purge` integration (D-22)
- Plugin chain installation order (RESEARCH.md "Plugin chain" Architecture Diagram lines 287-296):
  1. `HttpCookies(storage = cookiesStorageProvider(accountId))`
  2. `ContentNegotiation` with `Json { ignoreUnknownKeys = true; isLenient = true; explicitNulls = false }` (D-09)
  3. `Logging` with `KermitKtorLogger` + `level = if (isDebug) LogLevel.ALL else LogLevel.NONE` (D-28) + `sanitizeHeader { ... }` block
  4. `HttpTimeout(connect=10s, request=30s, socket=15s)`
  5. `HttpRequestRetry(maxRetries=5, exponentialDelay base=2.0 baseDelayMs=1000 maxDelayMs=30000, retry on 429/502/503/504)` (D-05)
  6. `DefaultRequest` with `url(baseUrl) + Accept-Language ru-RU + Accept JSON + X-Requested-With XMLHttpRequest + UserAgent desktop Safari` (RESEARCH.md Pitfall #1 risk note)
  7. `plugin(HttpSend).intercept` for 401 auto-relogin (D-26 — extracted into separate `AversAuthInterceptor.kt` per Code Example #5)

**KDoc must reference:** D-10 (plugin chain in `:core:network`, AVERS-specifics in `:core:api-avers-v4`), D-26 (401 re-login), Pitfall #1 (mobile UA divergence), Pitfall #4 (skip login endpoint), Pitfall #6 (per-account isolation invariant).

---

### `core/network/src/commonMain/kotlin/.../core/network/cookies/RoomCookiesStorage.kt` (adapter, request-response)

**Analog:** RESEARCH.md Pattern 2 (lines 595-654) — verbatim. NO Phase 1 internal analog.

**Direct copy targets:**
```kotlin
class RoomCookiesStorage(
    private val dao: CookieDao,
    private val accountId: String,
    private val clock: Clock = Clock.System,
) : CookiesStorage {
    override suspend fun get(requestUrl: Url): List<Cookie> {
        val now = clock.now()
        return dao.findFor(accountId = accountId, host = requestUrl.host, path = requestUrl.encodedPath)
            .filter { it.expiresAtEpochMillis == null || it.expiresAtEpochMillis > now.toEpochMilliseconds() }
            .map { it.toKtorCookie() }
    }

    override suspend fun addCookie(requestUrl: Url, cookie: Cookie) {
        val entity = cookie.toEntity(accountId, defaultDomain = requestUrl.host)
        dao.upsert(entity)
    }

    override fun close() { /* DAO does not own the database */ }
}
```

**Pitfall #7 (cookie domain matching) — DAO query pattern (lines 645-647):**
```kotlin
@Query("SELECT * FROM cookies WHERE accountId = :accountId AND :host LIKE '%' || domain AND :path LIKE path || '%'")
suspend fun findFor(accountId: String, host: String, path: String): List<CookieEntity>
```
Subdomain-suffix matching via `LIKE '%' || domain` — RFC 6265 leading-dot subdomain rule.

---

### `core/network/src/commonMain/kotlin/.../core/network/plugins/HttpRequestRedactor.kt` (utility, transform)

**Analog:** RESEARCH.md Code Example #4 (lines 922-953) — verbatim D-28 mandatory addendum.

**Direct copy:**
```kotlin
class HttpRequestRedactor {
    private val sensitiveKeys = listOf("password", "pwd", "pass", "cookie", "authorization", "set-cookie", "token")

    fun redact(body: String, contentType: String?): String {
        var result = body
        sensitiveKeys.forEach { key ->
            // Form-encoded: name=value
            result = result.replace(Regex("(?i)(\\b$key=)[^&\\s]*"), "$1***REDACTED***")
            // JSON: "name": "value"
            result = result.replace(Regex("(?i)(\"$key\"\\s*:\\s*\")[^\"]*"), "$1***REDACTED***")
        }
        return result
    }

    fun redactHeaderValue(headerName: String, headerValue: String): String =
        if (sensitiveKeys.any { it.equals(headerName, ignoreCase = true) }) "***REDACTED***"
        else headerValue
}
```

**KDoc must reference Pitfall #2** (regex over-match on benign content like "You forgot your password to the website"). Tests must include both sensitive-key (`password=secret123`) and benign-content (`"Note: changed password yesterday"`) canaries to prove the redactor matches keys, not arbitrary substrings.

---

### `core/network/src/commonMain/kotlin/.../core/network/plugins/AversAuthInterceptor.kt` (interceptor, request-response)

**Analog:** RESEARCH.md Code Example #5 (lines 960-991) — verbatim D-26 with Pitfall #4 mitigation.

**Direct copy:**
```kotlin
fun HttpClient.installAversAuthInterceptor(
    accountId: String,
    credentialProvider: CredentialProvider,
    loginCall: suspend (login: String, password: String) -> Boolean,
) {
    plugin(HttpSend).intercept { request ->
        // Pitfall #4 mitigation: skip interceptor for the login endpoint itself
        if (request.url.encodedPath.contains("/login", ignoreCase = true)) {
            return@intercept execute(request)
        }

        val originalCall = execute(request)
        if (originalCall.response.status != HttpStatusCode.Unauthorized) {
            return@intercept originalCall
        }

        val creds = credentialProvider.get(accountId)
            ?: return@intercept originalCall  // → caller maps 401 to AversApiError.Unauthorized

        val ok = loginCall(creds.first, creds.second)
        if (!ok) return@intercept originalCall  // re-login failed → caller sees 401 → Unauthorized

        execute(request)  // Replay; HttpCookies has new session cookies
    }
}
```

**Test analog:** RESEARCH.md Wave 0 Gap "AversAuthInterceptorTest.kt — stateful MockEngine 401-then-200 sequence; AND interceptor-skip-on-login-endpoint" (line 1278).

---

### `core/network/src/commonMain/kotlin/.../core/network/credentials/CredentialProvider.kt` (interface, request-response)

**Analog:** `core/platform/src/commonMain/kotlin/.../UrlOpener.kt` (KDoc structure with Phase boundaries + security note + TODO marker)

**Phase 1 KDoc structure** (`UrlOpener.kt:3-14`):
```kotlin
/**
 * Открывает URL в системном браузере.
 *
 * Phase 1: используется кнопкой «Открыть»...
 * Phase 4+: реиспользуется для всех внешних ссылок...
 *
 * Security note (Phase 4 TODO): на момент Phase 1 единственный caller — Hello LinTech composable...
 */
expect fun openUrl(url: String)
```

**Phase 2 adaptation** (per D-26):
```kotlin
package io.github.chudoxl.linteh.journal.core.network.credentials

/**
 * Provides AVERS credentials for auto-relogin (D-26).
 *
 * Phase 2: noop/test-only implementation supplied by tests via Mokkery.
 * Phase 3: KVault-backed implementation from `:core:platform` (or DI'd from `:feature:auth`)
 *    will replace any Phase 2 wiring stubs. The interface contract MUST NOT change between
 *    Phase 2 and Phase 3 — it's the boundary that Phase 3 fills in.
 *
 * Security note: returns `null` when credentials are not available; never throws.
 * Caller (AversAuthInterceptor) maps `null` → AversApiError.Unauthorized so the user
 * is presented with the login screen.
 *
 * Per-account scope: implementations MUST honor `accountId` — Phase 5 multi-account
 * scenarios depend on this.
 */
interface CredentialProvider {
    suspend fun get(accountId: String): Pair<String, String>?
}
```

NOT an `expect/actual` — pure interface, since Phase 3 will inject a concrete impl via DI rather than at compile time.

---

### `core/network/src/commonMain/kotlin/.../core/network/lifecycle/AccountDataPurger.kt` (interface + impl, event-driven)

**Analog:** RESEARCH.md Pattern 1 `HttpClientFactory.evict(accountId)` lines 515-517 + D-22 (no full code example).

**Implementation pattern synthesized from D-22 + Pattern 1:**
```kotlin
interface AccountDataPurger {
    suspend fun purge(accountId: String)
}

class DefaultAccountDataPurger(
    private val httpClientFactory: HttpClientFactory,
    private val cookieDao: CookieDao,
    private val databaseFileResolver: DatabaseFileResolver,  // expect/actual? or platform-supplied path
) : AccountDataPurger {
    override suspend fun purge(accountId: String) {
        // 1. Close & evict the HttpClient (releases cookies, scope, connections)
        httpClientFactory.evict(accountId)
        // 2. Delete cookie rows for this account
        cookieDao.deleteByAccount(accountId)
        // 3. Delete the per-account DB file (Phase 5 multi-account hygiene)
        databaseFileResolver.delete("journal_${accountId}.db")
    }
}
```

**Note:** `DatabaseFileResolver` is a small expect/actual helper for filesystem access — analog `core/platform/.../UrlOpener.kt` expect declaration shape. Planner should consider whether to put it in `:core:platform` (cross-cutting fs) or `:core:database` (where the DB lives). Recommendation: `:core:database` — ownership stays with the data module.

---

### `core/api-avers-v4/build.gradle.kts` (config, build-graph)

**Analog:** `core/platform/build.gradle.kts` (KMP-only, no compose) + `core/network/build.gradle.kts` (Phase 1 skeleton being filled in parallel)

**Plugins:**
```kotlin
plugins {
    id("lintech-kmp")
    id("lintech-test")
}
```
NO ksp / room (this module has no Room entities). NO compose (non-UI).

**KMP block** (analog to `core/network/build.gradle.kts:6-14`):
```kotlin
kotlin {
    androidTarget()
    listOf(iosX64(), iosArm64(), iosSimulatorArm64()).forEach { iosTarget ->
        iosTarget.binaries.framework {
            baseName = "apiAversV4"
            isStatic = true
        }
    }

    sourceSets {
        commonMain.dependencies {
            implementation(libs.ktor.client.core)
            implementation(libs.ktor.client.content.negotiation)
            implementation(libs.ktor.serialization.json)
            implementation(libs.kotlinx.serialization.json)
            implementation(libs.kotlinx.datetime)
            implementation(libs.kotlinx.coroutines.core)
            implementation(libs.kermit)
            implementation(project(":core:network"))
        }
        commonTest.dependencies {
            implementation(libs.ktor.client.mock)
        }
    }
}

android {
    namespace = "io.github.chudoxl.linteh.journal.core.api.avers.v4"
    compileSdk = 35
    defaultConfig { minSdk = 26 }
}
```

---

### `core/api-avers-v4/src/commonMain/kotlin/.../core/api/avers/v4/AversApi.kt` (service, request-response)

**Analog:** RESEARCH.md Pattern 3 (lines 663-701) — verbatim `fetchGrades` template applied to all 6 endpoints. NO Phase 1 internal analog.

**Direct pattern (per-endpoint scaffold, repeated 6×):**
```kotlin
suspend fun fetchGrades(accountId: String, period: Period): ApiResult<List<GradeDto>> = runCatching {
    val raw: JsonElement = httpClient.get("/journal/getMarks") {  // EXACT URL TBD AFTER HAR CAPTURE
        parameter("from", period.from.toString())
        parameter("to", period.to.toString())
    }.body()

    if (antiBotDetector.isCaptcha(raw)) {
        return ApiResult.Failure(AversApiError.AntiBotChallenge)  // OR data class with rawHtml — see Open Q #6
    }

    val envelope = raw.jsonObject
    val success = envelope["success"]?.jsonPrimitive?.boolean
        ?: return ApiResult.Mismatch(raw.toString(), listOf("success"), "grades")

    if (!success) return ApiResult.Failure(AversApiError.Server(httpCode = 200))

    val data = envelope["data"]
        ?: return ApiResult.Mismatch(raw.toString(), listOf("data"), "grades")

    val grades: List<GradeDto> = json.decodeFromJsonElement(ListSerializer(GradeDto.serializer()), data)
    ApiResult.Success(grades)
}.getOrElse { e ->
    when (e) {
        is HttpRequestTimeoutException, is IOException -> ApiResult.Failure(AversApiError.Network)
        is SerializationException -> ApiResult.Mismatch("(serialization-failed)", listOf(e.message ?: "?"), "grades")
        is ResponseException -> ApiResult.Failure(AversApiError.Server(e.response.status.value))
        else -> ApiResult.Failure(AversApiError.Network)
    }
}
```

**Hard-coded reminder for planner:** Endpoint URLs (`/journal/getMarks` etc.) are **placeholders** until first HAR capture (RESEARCH.md Open Question #1). Planner must order tasks: HAR capture → `aversApiV4_23813.md` documentation → endpoint code. Researcher cannot pre-write the URLs.

---

### `core/api-avers-v4/src/commonMain/kotlin/.../core/api/avers/v4/result/{ApiResult,AversApiError}.kt` (model, type-only)

**Analog:** RESEARCH.md Code Example #6 (lines 999-1014) — verbatim D-11.

**Direct copy:**
```kotlin
sealed class ApiResult<out T> {
    data class Success<T>(val value: T) : ApiResult<T>()
    data class Mismatch(val rawDump: String, val missingFields: List<String>, val endpoint: String) : ApiResult<Nothing>()
    data class Failure(val error: AversApiError) : ApiResult<Nothing>()
}

sealed class AversApiError {
    object Unauthorized : AversApiError()
    object AntiBotChallenge : AversApiError()  // RECOMMEND: data class AntiBotChallenge(val rawHtml: String) per Open Q #6
    object Network : AversApiError()
    data class ContractMismatch(val rawDump: String, val missingFields: List<String>, val endpoint: String) : AversApiError()
    data class Server(val httpCode: Int) : AversApiError()
    object KillSwitchTriggered : AversApiError()
}
```

**Open Question #6 (D-11 vs D-06 reconciliation):** Planner should change `object AntiBotChallenge` → `data class AntiBotChallenge(val rawHtml: String)` — strict superset, no breakage, supports Phase 3 WebView fallback (D-06).

---

### `core/api-avers-v4/src/commonMain/kotlin/.../core/api/avers/v4/killswitch/KillSwitchClient.kt` (service, request-response)

**Analog:** RESEARCH.md Code Example #7 (lines 1023-1059) — verbatim D-12, D-13, D-14.

**Direct copy:**
```kotlin
@Serializable
data class KillSwitchConfig(
    val latestSupportedAversBuild: String,
    val message: String = "",
    val severity: Severity = Severity.info,
    val minAppVersion: String = "0.0.0",
)

@Serializable
enum class Severity { info, warning, block }

class KillSwitchClient(
    private val httpClient: HttpClient,
    private val killSwitchUrl: String = "https://chudoxl.github.io/LintehJournal/api-config.json",
    private val currentAversBuild: String = "23813",
    private val currentAppVersion: String,
) {
    private var cached: KillSwitchConfig? = null

    suspend fun checkOrFailOpen(): ApiResult<KillSwitchConfig> {
        cached?.let { return ApiResult.Success(it) }
        return runCatching {
            val cfg: KillSwitchConfig = httpClient.get(killSwitchUrl) {
                timeout { requestTimeoutMillis = 5_000 }
            }.body()
            cached = cfg
            if (cfg.severity == Severity.block) ApiResult.Failure(AversApiError.KillSwitchTriggered)
            else ApiResult.Success(cfg)
        }.getOrElse {
            ApiResult.Success(KillSwitchConfig(latestSupportedAversBuild = currentAversBuild))  // D-13 fail-open
        }
    }
}
```

**Hardening refinement (RESEARCH.md Security Domain line 1324):** Honor `severity=block` only if `latestSupportedAversBuild != currentAversBuild`. Add this guard.

---

### `core/api-avers-v4/src/commonTest/kotlin/.../fixtures/HarReplayMockEngine.kt` (test utility, file-I/O)

**Analog:** RESEARCH.md Code Example #9 (lines 1156-1190) — verbatim. NO Phase 1 internal test analog.

**Direct copy:**
```kotlin
fun harMockEngine(harResourcePath: String): MockEngine {
    val har = loadResourceAsString(harResourcePath)
    val log = Json.parseToJsonElement(har).jsonObject["log"]!!.jsonObject
    val entries = log["entries"]!!.jsonArray.map { it.jsonObject }

    return MockEngine { request ->
        val match = entries.firstOrNull { e ->
            val req = e["request"]!!.jsonObject
            req["method"]!!.jsonPrimitive.content == request.method.value
                && request.url.toString().endsWith(
                    req["url"]!!.jsonPrimitive.content.substringAfter("//").substringAfter("/")
                )
        } ?: error("HAR fixture has no entry matching ${request.method} ${request.url}")

        val resp = match["response"]!!.jsonObject
        respond(
            content = ByteReadChannel(resp["content"]!!.jsonObject["text"]!!.jsonPrimitive.content),
            status = HttpStatusCode.fromValue(resp["status"]!!.jsonPrimitive.int),
            headers = headersOf(
                HttpHeaders.ContentType,
                resp["content"]!!.jsonObject["mimeType"]!!.jsonPrimitive.content,
            ),
        )
    }
}
```

**`loadResourceAsString` helper** is platform-specific — Phase 2 may need an `expect/actual` (NSBundle on iOS, classloader resources on Android). Mark as Phase-2 platform-abstraction extension (similar shape to `:core:platform` UrlOpener pattern).

---

### `composeApp/src/{androidUnitTest,iosTest}/.../*Test.kt` (test analogs for new modules)

**Analog (Android Robolectric):** `composeApp/src/androidUnitTest/kotlin/io/github/chudoxl/linteh/journal/AppTestAndroid.kt` — pattern for Robolectric-based tests of platform-actual code (e.g., `DatabaseFactory.android` file creation).

**Analog (iOS native):** `composeApp/src/iosTest/kotlin/io/github/chudoxl/linteh/journal/AppTest.kt` — pattern for `iosX64Test` tests in CI macos-15.

**Analog (commonTest helpers):** `composeApp/src/commonTest/kotlin/io/github/chudoxl/linteh/journal/AppTestHelpers.kt` — pattern for shared test helpers across iosTest + androidUnitTest (avoids drift).

**Phase 2 application:** For `:core:database` `DatabaseFactory` tests, mirror the AppTest split — common assertions in helper, platform-specific entry-points in `androidUnitTest` (Robolectric for Android filesystem) and `iosTest` (NSDocumentDirectory verification on macos-15 runner).

---

### `tools/sanitize-har.py` (utility, file-I/O / transform)

**Analog:** RESEARCH.md Code Example #8 (lines 1064-1149) — verbatim Python skeleton. NO Phase 1 internal analog.

Direct copy in full (Python file; not Kotlin) — see RESEARCH.md lines 1062-1150.

**Companion `tools/sanitize-rules.yaml`** — referenced as `Path(__file__).parent / "sanitize-rules.yaml"`. Schema documented inline in the script's docstring.

---

### `gradle/libs.versions.toml` (config, version catalog) — MODIFY

**Analog (self):** existing `gradle/libs.versions.toml` (Phase 1 baseline)

**Current state (lines 1-23):**
```toml
[versions]
kotlin = "2.2.20"
ksp = "2.2.20-2.0.4"
composeMultiplatform = "1.10.3"
agp = "8.7.3"
kotlinxSerialization = "1.7.3"     # Phase 2 BUMP target → 1.9.0
kotlinxDatetime = "0.6.2"          # Phase 2 BUMP target → 0.8.0-rc01
kotlinxCoroutines = "1.10.2"
# ... existing ...
```

**Phase 2 diff** — RESEARCH.md "Standard Stack Installation diff" (lines 209-243):
```toml
[versions]
# Bumped:
kotlinxSerialization = "1.9.0"      # from 1.7.3
kotlinxDatetime = "0.8.0-rc01"      # from 0.6.2 (or 0.7.x stable per A7)

# New:
ktor = "3.4.3"
room = "2.8.4"
sqlite = "2.6.2"

[libraries]
ktor-client-core = { module = "io.ktor:ktor-client-core", version.ref = "ktor" }
ktor-client-content-negotiation = { module = "io.ktor:ktor-client-content-negotiation", version.ref = "ktor" }
ktor-serialization-json = { module = "io.ktor:ktor-serialization-kotlinx-json", version.ref = "ktor" }
ktor-client-logging = { module = "io.ktor:ktor-client-logging", version.ref = "ktor" }
ktor-client-darwin = { module = "io.ktor:ktor-client-darwin", version.ref = "ktor" }
ktor-client-okhttp = { module = "io.ktor:ktor-client-okhttp", version.ref = "ktor" }
ktor-client-mock = { module = "io.ktor:ktor-client-mock", version.ref = "ktor" }

androidx-room-runtime = { module = "androidx.room:room-runtime", version.ref = "room" }
androidx-room-compiler = { module = "androidx.room:room-compiler", version.ref = "room" }
androidx-sqlite-bundled = { module = "androidx.sqlite:sqlite-bundled", version.ref = "sqlite" }
androidx-room-sqlite-wrapper = { module = "androidx.room:room-sqlite-wrapper", version.ref = "room" }

[plugins]
androidxRoom = { id = "androidx.room", version.ref = "room" }
```

`ksp` plugin alias already exists (line 57) — do NOT redeclare.

---

### `settings.gradle.kts` (config, build-graph) — MODIFY

**Analog (self):** existing `settings.gradle.kts` (lines 17-23)

```kotlin
include(":composeApp")
include(":core:platform")
include(":core:ui")
include(":core:network")
```

Phase 2 add:
```kotlin
include(":core:database")
include(":core:api-avers-v4")
```

---

### `.github/workflows/ci.yml` (config, CI) — MODIFY

**Analog (self):** existing `.github/workflows/ci.yml` (Phase 1 baseline)

**Existing Android job structure** (`ci.yml:17-48`):
```yaml
android:
  runs-on: ubuntu-latest
  steps:
    - Checkout / Set up JDK / Setup Gradle
    - name: Assemble debug
      run: ./gradlew assembleDebug
    - name: Lint
      run: ./gradlew lint
    - name: Run Android JVM tests (debug)
      run: ./gradlew testDebugUnitTest
```

**Phase 2 additions** (RESEARCH.md Wave 0 Gaps lines 1283-1285):
```yaml
- name: Sanitize HAR canary lint
  run: bash tests/sanitize-har-canary.sh

- name: Log redactor canary (debug-build per D-28)
  run: bash tests/log-redactor-canary.sh
```

**Existing iOS job** runs `:composeApp:iosX64Test` + `linkDebugFrameworkIosX64` + `Lint PrivacyInfo.xcprivacy`. Phase 2 adds:
```yaml
- name: Run iOS X64 tests for new modules
  run: ./gradlew :core:database:iosX64Test :core:network:iosX64Test :core:api-avers-v4:iosX64Test
```

**Privacy Manifest lint chain** (`ci.yml:100-137`) — Phase 2 may need to add reason codes for Room/Ktor required-reason API usage (RESEARCH.md "iOS Privacy Manifest" line 161). Add new `grep` lines for any newly required codes (e.g., 0A2A.1 for fstat, 35F9.1 for mach_absolute_time per JetBrains issue #4738 in CLAUDE.md §Privacy & compliance).

---

## Shared Patterns

### Logging via Kermit + TAG constant
**Source:** `core/platform/src/iosMain/kotlin/.../UrlOpener.ios.kt:13` + `core/platform/src/androidMain/kotlin/.../UrlOpener.android.kt:51`
**Apply to:** All Phase 2 platform-impl files + plugin chain logger
```kotlin
import co.touchlab.kermit.Logger

private const val TAG = "ModuleOrComponentName"

// Usage:
Logger.w(TAG) { "message $variable" }
Logger.w(TAG, throwable) { "message" }
```
Phase 2 applications:
- `DatabaseFactory.{android,ios}.kt` — log DB path on creation
- `HttpClientFactory.kt` — `KermitKtorLogger` adapter for Ktor `Logging` plugin (D-28)
- `KillSwitchClient.kt` — log fail-open events
- `AversAuthInterceptor.kt` — log re-login attempts (NEVER log credentials — redactor must run)

---

### expect/actual platform abstraction
**Source:** `core/platform/.../UrlOpener.kt` (commonMain expect) + `UrlOpener.android.kt` + `UrlOpener.ios.kt` (actuals)
**Apply to:** `DatabaseFactory.kt` (D-19) + `JournalDatabaseConstructor.kt` (Room KMP standard) + optionally `DatabaseFileResolver` (for `AccountDataPurger`) + optionally `loadResourceAsString` helper (for HarReplayMockEngine).

**Pattern:**
1. `expect fun X(...)` or `expect object X { fun ... }` in `commonMain` with KDoc explaining Phase boundaries + Security note + TODO for future phases
2. `actual` in `androidMain/.../X.android.kt` with `import android.*` + Kermit Logger + defensive runCatching
3. `actual` in `iosMain/.../X.ios.kt` with `import platform.Foundation.*` + Kermit Logger + defensive null-checks

**KDoc skeleton (always include all four sections):**
```kotlin
/**
 * <one-line purpose>.
 *
 * Phase X: <current usage>.
 * Phase Y+: <future expansion>.
 *
 * Security note: <invariants this preserves>.
 *
 * TODO(Phase Z): <known evolution path>.
 */
```

---

### KMP module build.gradle.kts skeleton (non-UI)
**Source:** `core/platform/build.gradle.kts` (lines 1-34)
**Apply to:** `core/database/build.gradle.kts` (with Room/KSP additions), `core/api-avers-v4/build.gradle.kts`
**Anti-pattern (Pitfall #8):** NEVER add `id("lintech-compose")` to non-UI modules.

```kotlin
plugins {
    id("lintech-kmp")
    id("lintech-test")
    // [optional Phase-2-specific plugins, e.g., ksp + androidxRoom for :core:database]
}

kotlin {
    androidTarget()
    listOf(iosX64(), iosArm64(), iosSimulatorArm64()).forEach { iosTarget ->
        iosTarget.binaries.framework {
            baseName = "<short-module-name>"
            isStatic = true
        }
    }

    sourceSets {
        commonMain.dependencies { /* ... */ }
        // [androidMain.dependencies, iosMain.dependencies as needed]
    }
}

android {
    namespace = "io.github.chudoxl.linteh.journal.core.<module>"
    compileSdk = 35
    defaultConfig { minSdk = 26 }
}
```

---

### Sealed-error + sealed-result pair for service boundaries
**Source:** RESEARCH.md Code Example #6 (lines 999-1014) — D-11 verbatim
**Apply to:** `:core:api-avers-v4` only (not `:core:network` — that uses Ktor's exception types, then maps at the API boundary).

Every endpoint function returns `ApiResult<T>` and never throws. Mapping table:
- `IOException` / `HttpRequestTimeoutException` → `ApiResult.Failure(AversApiError.Network)`
- `SerializationException` → `ApiResult.Mismatch(rawDump, missingFields, endpoint)`
- `ResponseException` → `ApiResult.Failure(AversApiError.Server(httpCode))`
- HTML response → `ApiResult.Failure(AversApiError.AntiBotChallenge)` (or `data class AntiBotChallenge(rawHtml)` per Open Q #6)
- 401 after re-login fails → `ApiResult.Failure(AversApiError.Unauthorized)`
- Kill-switch `severity=block` → `ApiResult.Failure(AversApiError.KillSwitchTriggered)`

---

### Test split (commonTest + iosTest + androidUnitTest)
**Source:**
- `composeApp/src/commonTest/.../AppTestHelpers.kt` (shared assertions)
- `composeApp/src/iosTest/.../AppTest.kt` (iosX64Test runner)
- `composeApp/src/androidUnitTest/.../AppTestAndroid.kt` (Robolectric runner)

**Apply to:** Any Phase 2 test that exercises platform-actual code (e.g., `DatabaseFactory` file creation, `loadResourceAsString` resource loading). Pure-commonMain logic (HttpRequestRedactor, ApiResult mapping, AntiBotDetector regex, KillSwitchClient with MockEngine) lives in `commonTest` only.

**Test command pattern** (CLAUDE.md §Build & verification):
- Local Android JVM: `./gradlew :core:<module>:test`
- Local iOS compile-only: `./gradlew :core:<module>:compileKotlinIosX64`
- CI iOS: `./gradlew :core:<module>:iosX64Test` (macos-15 runner)

---

### Mokkery for KMP-friendly mocks
**Source:** `build-logic/convention/src/main/kotlin/LintechTestConventionPlugin.kt:11` (`pluginManager.apply("dev.mokkery")`)
**Apply to:** All Phase 2 commonTest files needing mocks (CookieDao mock, CredentialProvider mock, KillSwitchConfig HttpClient mock).

`lintech-test` plugin already wires Mokkery — modules apply `id("lintech-test")` and Mokkery's `@Mock`/`every`/`mock` API is available in commonTest.

**Anti-pattern:** Do NOT use MockK on iOS targets (RESEARCH.md "What NOT to Use" + STACK.md). Mokkery is mandatory for KMP mocks.

---

### CI lint chain (multi-step shell verification)
**Source:** `.github/workflows/ci.yml:100-137` (Privacy Manifest plutil lint chain, 5 steps with explicit error messages and `set -euo pipefail`)

**Apply to:** `tests/sanitize-har-canary.sh` and `tests/log-redactor-canary.sh`. Pattern:
```bash
#!/usr/bin/env bash
set -euo pipefail

echo "Step 1: <description>"
<command> || { echo "ERROR: <specific failure mode>"; exit 1; }

echo "Step 2: <description>"
# ...

echo "All checks passed."
```

For `sanitize-har-canary.sh`: grep `fixtures/sanitized/` for canary names that should NOT exist (real ФИО) — fail if found.

For `log-redactor-canary.sh`: run `./gradlew :core:network:test --rerun-tasks` capturing stdout, then grep for `kanareyka_PASSWORD_DO_NOT_LEAK_42` — fail if found (must NOT appear; redactor should mask it).

---

## No Analog Found

**No "missing analog" cases.** Every Phase 2 file maps to either:
1. A Phase 1 internal pattern (UrlOpener expect/actual, build.gradle.kts skeleton, AppTest split, Kermit Logger, AndroidManifest fragment, libs.versions.toml structure, ci.yml step shape, convention plugin application)
2. A verbatim RESEARCH.md code example (Patterns 1-3, Code Examples 1-9)
3. A self-analog (extending in place: libs.versions.toml, settings.gradle.kts, ci.yml, PrivacyInfo.xcprivacy)

Phase 2 is the project's **first contact with Ktor and Room**, so most "core" code is RESEARCH-driven rather than existing-code-driven — but every shape (KMP module skeleton, expect/actual, test split, logging, KDoc structure) inherits from Phase 1 conventions.

---

## Metadata

**Analog search scope:**
- `core/platform/` (Phase 1 reference KMP module)
- `core/network/` (Phase 1 skeleton — being filled)
- `core/ui/` (Phase 1 reference UI module — for non-application of compose-deps anti-pattern)
- `composeApp/` (Phase 1 reference app module — for AppTest split, AndroidManifest, BuildKonfig)
- `build-logic/convention/` (all 3 convention plugins + ext/)
- `gradle/libs.versions.toml`
- `settings.gradle.kts`
- `.github/workflows/ci.yml`
- `composeApp/PrivacyInfo.xcprivacy`
- `.planning/phases/02-api-reverse-engineering-network-layer/02-RESEARCH.md` (1450 lines, full read in non-overlapping segments)
- `.planning/phases/02-api-reverse-engineering-network-layer/02-CONTEXT.md` (D-01..D-28)
- `.planning/phases/01-foundation-compliance-infrastructure/01-CONTEXT.md` (D-01..D-31, especially D-31 expect/actual + BLOCKER 1 fix iter 1)
- `CLAUDE.md` (project conventions, package root, transliteration policy)

**Files scanned:** 18 source / config files + 3 planning context files

**Pattern extraction date:** 2026-04-28

## PATTERN MAPPING COMPLETE

**Phase:** 2 — API Reverse-Engineering & Network Layer
**Files classified:** 46 (across `:core:database` × 13, `:core:network` × 14 [includes 1 modify], `:core:api-avers-v4` × 16, top-level × 13)
**Analogs found:** 14 distinct analog sources (8 from `core/platform/` + 6 from `composeApp/` + version-catalog/CI/manifest self-analogs) covering 100% of file roles

### Coverage
- Files with exact Phase 1 analog: 24 (KMP build files, expect/actual triples, manifest fragment, KDoc structure, test split, Kermit logging, libs.versions.toml extensions, settings.gradle.kts extensions, ci.yml step extensions)
- Files with verbatim RESEARCH.md code example: 18 (HttpClientFactory, RoomCookiesStorage, HttpRequestRedactor, AversAuthInterceptor, ApiResult, AversApiError, KillSwitchClient, KillSwitchConfig, HarReplayMockEngine, sanitize-har.py, fetchGrades pattern × 6 endpoints, manifest fragment for `:core:database`)
- Files with no analog (purely RESEARCH-driven, first-of-kind): 4 (`AccountDataPurger`, `AntiBotDetector`, `AversEnvelope`, `aversApiV4_23813.md` — but each has architectural justification in RESEARCH.md)

### Key Patterns Identified
- **All non-UI KMP modules apply only `lintech-kmp + lintech-test`** — Pitfall #8 / BLOCKER 1 fix iter 1 (no compose-deps drag into `:core:database` or `:core:api-avers-v4`); confirmed against `core/platform/build.gradle.kts` analog (no `lintech-compose`).
- **expect/actual pattern (D-31) applies uniformly** — `DatabaseFactory` (commonMain expect / android+ios actual), `JournalDatabaseConstructor` (Room KMP standard), and optionally `DatabaseFileResolver` + `loadResourceAsString` helper. All follow `UrlOpener` template (KDoc structure + private TAG + Kermit Logger + defensive runCatching/null-check).
- **Sealed `ApiResult<T>` + `AversApiError` boundary at `:core:api-avers-v4`** — service functions never throw; all errors map to typed sealed members at the endpoint boundary. Ktor exceptions stay inside `:core:network` plumbing.
- **Per-account scope as architectural invariant** — `HttpClientFactory.forAccount(id)` cache + `RoomCookiesStorage(accountId)` + `journal_${accountId}.db` filename + `AccountDataPurger.purge(accountId)`. Phase 2 with `accountId="default"` already enforces the pattern.
- **Logging hygiene is multi-layered** — `sanitizeHeader` for Auth/Cookie/Set-Cookie + `HttpRequestRedactor` regex for bodies (mandatory in debug per D-28 addendum) + `LogLevel.NONE` in release + CI canary script `tests/log-redactor-canary.sh` greps for `kanareyka_PASSWORD_DO_NOT_LEAK_42` in debug-build test output.
- **Test analogs split across iosTest + androidUnitTest + commonTest** — common helpers/assertions in commonTest (analog `AppTestHelpers.kt`), platform-runners in iosTest (`AppTest.kt` shape) and androidUnitTest (Robolectric `AppTestAndroid.kt` shape). Pure commonMain logic stays in commonTest only.

### File Created
`.planning/phases/02-api-reverse-engineering-network-layer/02-PATTERNS.md`

### Ready for Planning
Pattern mapping complete. Planner can now reference analog patterns directly in PLAN.md files: each new file points to either a Phase 1 internal analog (concrete file path + line numbers) or a verbatim RESEARCH.md code example (line range), with shared cross-cutting patterns (Kermit logging, expect/actual, sealed errors, test split, no-compose-deps) factored out for reuse across multiple plans.
